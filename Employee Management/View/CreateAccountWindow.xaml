<Window x:Class="Employee_Management.View.CreateAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Tạo Tài Khoản" Height="400" Width="400" WindowStartupLocation="CenterScreen"
        Background="#2C3E50">
    <Grid>
        <Border Background="#34495E" CornerRadius="10" BorderBrush="#1ABC9C" BorderThickness="2"
                HorizontalAlignment="Center" VerticalAlignment="Center" Width="350" Padding="20">
            <StackPanel>
                <!-- Tiêu đề -->
                <TextBlock Text="Tạo Tài Khoản" FontSize="20" FontWeight="Bold" 
                           Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Username -->
                <TextBlock Text="Username:" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,5,0,5"/>
                <TextBox x:Name="txtUsername" Height="35" FontSize="14" Padding="5" 
                         Background="#ECF0F1" BorderBrush="#1ABC9C" BorderThickness="1"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Password -->
                <TextBlock Text="Password:" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,10,0,5"/>
                <PasswordBox x:Name="txtPassword" Height="35" FontSize="14" Padding="5" 
                 Background="#ECF0F1" BorderBrush="#1ABC9C" BorderThickness="1"
                PasswordChanged="txtPassword_PasswordChanged"/>

                <!-- Nút Tạo và Hủy -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Tạo" Width="100" Height="35" Background="#1ABC9C" 
                    Foreground="White" FontWeight="Bold" BorderBrush="Transparent" 
                    Cursor="Hand" Margin="0,0,10,0"
                    Command="{Binding CreateAccountCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

                    <Button Content="Hủy" Width="100" Height="35" Background="#E74C3C" 
                    Foreground="White" FontWeight="Bold" BorderBrush="Transparent" 
                    Cursor="Hand" Margin="10,0,0,0"
                    Command="{Binding CancelCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>