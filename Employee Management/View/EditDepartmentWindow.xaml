<Window x:Class="Employee_Management.View.EditDepartmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chỉnh sửa phòng ban" Height="200" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
   
    <Grid Margin="20">
        <StackPanel>
            <!-- Trường nhập tên phòng ban -->
            <TextBlock Text="Tên phòng ban:" FontSize="16" Margin="0,0,0,10"/>
            <TextBox x:Name="txtDepartmentName" FontSize="16" Padding="5" Margin="0,0,0,20"
                     Text="{Binding UpdatedDepartment.DepartmentName, UpdateSourceTrigger=PropertyChanged}"/>

            <!-- Nút Lưu và Hủy -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Lưu" Width="80" Margin="0,0,10,0"
                        Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
                <Button Content="Hủy" Width="80"
                        Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>