<Window x:Class="Employee_Management.View.EditProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Profile" Height="600" Width="800" WindowStartupLocation="CenterScreen"
        Background="#2C3E50">
    <Grid>
        <Border Background="#34495E" CornerRadius="10" BorderBrush="#1ABC9C" BorderThickness="2"
                HorizontalAlignment="Center" VerticalAlignment="Center" Width="500" Padding="20">
            <StackPanel>
                <!-- Tiêu đề -->
                <TextBlock Text="Chỉnh sửa hồ sơ" FontSize="20" FontWeight="Bold" 
                           Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Full Name -->
                <TextBlock Text="Họ và tên:" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,5,0,5"/>
                <TextBox x:Name="txtFullName" Height="35" FontSize="14" Padding="5" 
                         Background="#ECF0F1" BorderBrush="#1ABC9C" BorderThickness="1"
                         Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Date of Birth -->
                <TextBlock Text="Ngày sinh:" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,10,0,5"/>
                <DatePicker x:Name="dpDateOfBirth" Height="35" FontSize="14" Padding="5" 
                            Background="#ECF0F1" BorderBrush="#1ABC9C" BorderThickness="1"
                            SelectedDate="{Binding DateOfBirth, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Gender -->
                <TextBlock Text="Giới tính:" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,10,0,5"/>
                <ComboBox x:Name="cbGender" 
                Height="35" 
                FontSize="14" 
                Padding="5" 
                Background="#ECF0F1" 
                BorderBrush="#1ABC9C" 
                BorderThickness="1"
                ItemsSource="{Binding Genders}"
                SelectedItem="{Binding Gender, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Address -->
                <TextBlock Text="Địa chỉ:" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,10,0,5"/>
                <TextBox x:Name="txtAddress" Height="35" FontSize="14" Padding="5" 
                         Background="#ECF0F1" BorderBrush="#1ABC9C" BorderThickness="1"
                         Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Phone Number -->
                <TextBlock Text="SĐT:" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,10,0,5"/>
                <TextBox x:Name="txtPhoneNumber" Height="35" FontSize="14" Padding="5" 
                         Background="#ECF0F1" BorderBrush="#1ABC9C" BorderThickness="1"
                         Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Nút Lưu và Hủy -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Lưu" 
                     Width="100" 
                        Height="35" 
                        Background="#1ABC9C" 
                    Foreground="White" 
                    FontWeight="Bold" 
                    BorderBrush="Transparent" 
                    Cursor="Hand" 
                    Margin="0,0,10,0"
                     Command="{Binding SaveCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

                    <Button Content="Huỷ" 
                Width="100" 
                Height="35" 
                Background="#E74C3C" 
                Foreground="White" 
                FontWeight="Bold" 
                BorderBrush="Transparent" 
                Cursor="Hand" 
                Margin="10,0,0,0"
                Command="{Binding CancelCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>