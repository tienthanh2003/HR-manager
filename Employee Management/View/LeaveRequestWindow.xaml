<Window x:Class="Employee_Management.View.LeaveRequestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Xin nghỉ phép" Height="400" Width="600" WindowStartupLocation="CenterScreen"
        Background="#2C3E50">
    <Grid>
        <Border Background="#34495E" CornerRadius="10" BorderBrush="#1ABC9C" BorderThickness="2"
                HorizontalAlignment="Center" VerticalAlignment="Center" Width="500" Padding="20">
            <StackPanel>
                <!-- Tiêu đề -->
                <TextBlock Text="Xin nghỉ phép" FontSize="20" FontWeight="Bold" 
                           Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Loại nghỉ phép -->
                <TextBlock Text="Loại nghỉ phép:" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,5,0,5"/>
                <ComboBox x:Name="cmbLeaveType" 
                          Height="35" 
                          FontSize="14" 
                          Padding="5" 
                          Background="#ECF0F1" 
                          BorderBrush="#1ABC9C" 
                          BorderThickness="1"
                          ItemsSource="{Binding LeaveTypes}"
                          SelectedItem="{Binding SelectedLeaveType, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Ngày bắt đầu -->
                <TextBlock Text="Ngày bắt đầu:" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,10,0,5"/>
                <DatePicker x:Name="dpStartDate" 
                            Height="35" 
                            FontSize="14" 
                            Padding="5" 
                            Background="#ECF0F1" 
                            BorderBrush="#1ABC9C" 
                            BorderThickness="1"
                            SelectedDate="{Binding StartDate, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Ngày kết thúc -->
                <TextBlock Text="Ngày kết thúc:" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,10,0,5"/>
                <DatePicker x:Name="dpEndDate" 
                            Height="35" 
                            FontSize="14" 
                            Padding="5" 
                            Background="#ECF0F1" 
                            BorderBrush="#1ABC9C" 
                            BorderThickness="1"
                            SelectedDate="{Binding EndDate, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Nút Gửi đơn và Hủy -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Gửi đơn" 
                            Width="100" 
                            Height="35" 
                            Background="#1ABC9C" 
                            Foreground="White" 
                            FontWeight="Bold" 
                            BorderBrush="Transparent" 
                            Cursor="Hand" 
                            Margin="0,0,10,0"
                            Command="{Binding SubmitLeaveRequestCommand}"
                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

                    <Button Content="Huỷ" 
                            Width="100" 
                            Height="35" 
                            Background="#E74C3C" 
                            Foreground="White" 
                            FontWeight="Bold" 
                            BorderBrush="Transparent" 
                            Cursor="Hand" 
                            Margin="10,0,0,0"
                            Command="{Binding CancelCommand}"
                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
    
</Window>
    