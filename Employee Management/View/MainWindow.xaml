<Window x:Class="Employee_Management.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Employee_Management.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="750" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50">
            <TextBlock Text="Quản trị hệ thống" FontSize="30" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Background="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <StackPanel Background="#2C3E50" Width="200" Height="Auto">
                <Button Command="{Binding ManageEmployeesCommand}" Background="Blue" Foreground="White" Height="50" Width="180" Margin="5">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" FontSize="20" Margin="5"/>
                        <TextBlock Text="Quản lý nhân viên" FontSize="16"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding ManageDepartmentsCommand}" Background="Blue" Foreground="White" Height="50" Width="180" Margin="5">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="&#xE7BE;" FontFamily="Segoe MDL2 Assets" FontSize="20" Margin="5"/>
                        <TextBlock Text="Quản lý phòng ban" FontSize="16"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding ManageSalariesCommand}" Background="Blue" Foreground="White" Height="50" Width="180" Margin="5">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="&#xE8D0;" FontFamily="Segoe MDL2 Assets" FontSize="20" Margin="5"/>
                        <TextBlock Text="Quản lý lương thưởng" FontSize="15"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding ManageLeaveCommand}" Background="Blue" Foreground="White" Height="50" Width="180" Margin="5">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="&#xE8F3;" FontFamily="Segoe MDL2 Assets" FontSize="20" Margin="5"/>
                        <TextBlock Text="Quản lý nghỉ phép" FontSize="15"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding ManageAttendanceCommand}" Background="Blue" Foreground="White" Height="50" Width="180" Margin="5">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="&#xE823;" FontFamily="Segoe MDL2 Assets" FontSize="20" Margin="5"/>
                        <TextBlock Text="Quản lý chấm công" FontSize="16"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding ReportsCommand}" Background="Blue" Foreground="White" Height="50" Width="180" Margin="5">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="&#xE9D2;" FontFamily="Segoe MDL2 Assets" FontSize="20" Margin="5"/>
                        <TextBlock Text="Báo cáo và Thống kê" FontSize="16"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding SendNotificationsCommand}" Background="Blue" Foreground="White" Height="50" Width="180" Margin="5" >
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="&#xE8BD;" FontFamily="Segoe MDL2 Assets" FontSize="20" Margin="5"/>
                        <TextBlock Text="Gửi thông báo" FontSize="16"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding BackupCommand}" Background="Blue" Foreground="White" Height="50" Width="180" Margin="5">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="&#xE74E;" FontFamily="Segoe MDL2 Assets" FontSize="20" Margin="5"/>
                        <TextBlock Text="Sao lưu" FontSize="16"/>
                    </StackPanel>
                </Button>

                <!-- Nút Phục hồi -->
                <Button Command="{Binding RestoreCommand}" Background="Blue" Foreground="White" Height="50" Width="180" Margin="5">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="20" Margin="5"/>
                        <TextBlock Text="Phục hồi" FontSize="16"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding ViewActivityLogCommand}" Background="Blue" Foreground="White" Height="50" Width="180" Margin="5">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="&#xE9F9;" FontFamily="Segoe MDL2 Assets" FontSize="20" Margin="5"/>
                        <TextBlock Text="Xem Log" FontSize="16"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding LogoutCommand}" Background="Red" Foreground="White" Height="50" Width="180" Margin="5">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="&#xE8AC;" FontFamily="Segoe MDL2 Assets" FontSize="20" Margin="5"/>
                        <TextBlock Text="Đăng xuất" FontSize="16"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- Main Content Area -->
            <Border Grid.Column="1" Background="LightGray" CornerRadius="10">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <!-- Dòng chào mừng -->
                    <TextBlock Text="Chào mừng đến với hệ thống quản lý nhân sự" 
                               FontSize="24" FontWeight="Bold" Foreground="Black" 
                               HorizontalAlignment="Center" Margin="0,0,0,20"/>

                    <!-- Phần hiển thị thông báo -->
                    <Border Background="White" CornerRadius="5" Padding="10" Width="500">
                        <StackPanel>
                            <TextBlock Text="Thông báo:" FontSize="18" FontWeight="Bold" Foreground="Black" Margin="0,0,0,10"/>
                            <ListBox ItemsSource="{Binding Notifications}" 
                                     Background="Transparent" BorderThickness="0" 
                                     Height="200" ScrollViewer.VerticalScrollBarVisibility="Auto">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="Bold" Foreground="Black"/>
                                            <TextBlock Text="{Binding Content}" FontSize="12" Foreground="Gray" Margin="5,0,0,0"/>
                                            <TextBlock Text="{Binding SentDate, StringFormat='dd/MM/yyyy HH:mm'}" FontSize="10" Foreground="DarkGray" Margin="5,0,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#2C3E50">
            <TextBlock Text="@2025 - Hệ thống quản lý nhân sự" FontSize="14" Foreground="White" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>