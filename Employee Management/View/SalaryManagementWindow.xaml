<Window x:Class="Employee_Management.View.SalaryManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Quản lý lương thưởng" Height="600" Width="1400" Background="#2C3E50">

    <Grid>
        <!-- Border chứa danh sách lương thưởng -->
        <Border Background="#34495E" CornerRadius="10" BorderBrush="#1ABC9C" BorderThickness="2"
                Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Thanh tìm kiếm và nút chức năng -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10" Grid.Row="0">
                    <!-- Label và TextBox tìm kiếm -->
                    <Label Content="Tìm Kiếm:" VerticalAlignment="Center" Foreground="White" FontWeight="Bold"/>
                    <TextBox x:Name="txtSearch" Width="200" Height="30" Padding="5"
                             Background="#ECF0F1" BorderBrush="#1ABC9C" BorderThickness="1"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

                    <!-- ComboBox để chọn trạng thái lương -->
                    <Label Content=" Trạng thái:" VerticalAlignment="Center" Foreground="White" FontWeight="Bold" Margin="10,0,0,0"/>
                    <ComboBox ItemsSource="{Binding SalaryStatuses}" 
                              SelectedItem="{Binding SelectedSalaryStatus, Mode=TwoWay}" 
                              Width="150" Height="30" Margin="10,0,0,0"/>

                    <!-- Các nút chức năng -->
                    <Button Content="Tìm" Width="100" Height="30" Margin="10,0,0,0"
                            Background="#1ABC9C" Foreground="White" FontWeight="Bold"
                            BorderBrush="Transparent" Cursor="Hand" Command="{Binding SearchCommand}"/>
                    <Button Content="Tải lại" Width="100" Height="30" Margin="10,0,0,0"
                            Background="#3498DB" Foreground="White" FontWeight="Bold"
                            BorderBrush="Transparent" Cursor="Hand" Command="{Binding LoadCommand}"/>
                    <Button Content="Quay lại" Width="120" Height="30" Margin="10,0,0,0"
                            Background="#E74C3C" Foreground="White" FontWeight="Bold"
                            BorderBrush="Transparent" Cursor="Hand" Command="{Binding BackCommand}"/>
                </StackPanel>

                <!-- DataGrid hiển thị danh sách lương thưởng -->
                <DataGrid x:Name="dgSalaries" Grid.Row="1" Margin="10" AutoGenerateColumns="False"
                          Background="Transparent" RowBackground="#ECF0F1" AlternatingRowBackground="#D5DBDB"
                          HeadersVisibility="Column" IsReadOnly="True" ItemsSource="{Binding Salaries}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Họ và tên" Binding="{Binding EmployeeName}" Width="150"/>
                        <DataGridTextColumn Header="Lương cơ bản" Binding="{Binding BaseSalary}" Width="100"/>
                        <DataGridTextColumn Header="Phụ cấp" Binding="{Binding Allowance}" Width="100"/>
                        <DataGridTextColumn Header="Thưởng" Binding="{Binding Bonus}" Width="100"/>
                        <DataGridTextColumn Header="Khấu trừ" Binding="{Binding Deduction}" Width="100"/>
                        <DataGridTextColumn Header="Lương tổng" Binding="{Binding TotalSalary}" Width="120"/>
                        <DataGridTextColumn Header="Ngày bắt đầu" Binding="{Binding StartDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                        <DataGridTextColumn Header="Ngày thanh toán" Binding="{Binding PaymentDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                        <DataGridTextColumn Header="Phép còn" Binding="{Binding RemainingLeaveDays}" Width="60"/>
                        <DataGridTextColumn Header="Công" Binding="{Binding WorkDays}" Width="60"/>
                        <DataGridTextColumn Header="Vắng" Binding="{Binding AbsentDays}" Width="60"/>
                        <DataGridTextColumn Header="Giờ tăng ca" Binding="{Binding OvertimeHours}"  Width="60"/>
                        <DataGridTextColumn Header="Trạng thái lương" Binding="{Binding SalaryStatus}" Width="100"/>
                        <!-- Cột Action (Edit/Delete) -->
                        <DataGridTemplateColumn Header="Thao tác" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="10,0,0,0">
                                        <Button Content="Sửa" Width="60" Margin="5,0"
                                                Background="#3498DB" Foreground="White" FontWeight="Bold"
                                                BorderBrush="Transparent" Cursor="Hand"
                                                Command="{Binding DataContext.UpdateCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"/>
                                        
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Footer -->
                <TextBlock Grid.Row="2" Text="Quản lý lương thưởng - Hệ thống quản lý nhân sự" HorizontalAlignment="Center"
                           Foreground="White" FontSize="12" Margin="10"/>
            </Grid>
        </Border>
    </Grid>
</Window>